Hello.

<%= @comment.user.email %> has just updated the <%= @comment.ticket.title %> ticket for <%= @comment.ticket.project.name %>. They wrote:

<%= simple_format(@comment.text) %>

You can view this ticket online by going to: <%= project_ticket_url(@comment.ticket.project, @comment.ticket) %>